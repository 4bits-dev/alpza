<?php

/**
 * Base class that represents a row from the 'alpza_comite' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Sat Nov 24 15:15:35 2012
 *
 * @package    lib.model.om
 */
abstract class BaseAlpzaComite extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        AlpzaComitePeer
	 */
	protected static $peer;

	/**
	 * The value for the id_alpza_comite field.
	 * @var        int
	 */
	protected $id_alpza_comite;

	/**
	 * The value for the id_alpza_miembro field.
	 * @var        int
	 */
	protected $id_alpza_miembro;

	/**
	 * The value for the nombre field.
	 * @var        string
	 */
	protected $nombre;

	/**
	 * The value for the mision field.
	 * @var        string
	 */
	protected $mision;

	/**
	 * The value for the vision field.
	 * @var        string
	 */
	protected $vision;

	/**
	 * The value for the objetivo field.
	 * @var        string
	 */
	protected $objetivo;

	/**
	 * The value for the informacion field.
	 * @var        string
	 */
	protected $informacion;

	/**
	 * The value for the email field.
	 * @var        string
	 */
	protected $email;

	/**
	 * The value for the coordinador field.
	 * @var        string
	 */
	protected $coordinador;

	/**
	 * The value for the email_coordinador field.
	 * @var        string
	 */
	protected $email_coordinador;

	/**
	 * The value for the estado field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $estado;

	/**
	 * The value for the fecha_ingreso field.
	 * @var        string
	 */
	protected $fecha_ingreso;

	/**
	 * The value for the fecha_edicion field.
	 * @var        string
	 */
	protected $fecha_edicion;

	/**
	 * The value for the fecha_publicacion field.
	 * @var        string
	 */
	protected $fecha_publicacion;

	/**
	 * @var        AlpzaMiembro
	 */
	protected $aAlpzaMiembro;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior

	const PEER = 'AlpzaComitePeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->estado = 1;
	}

	/**
	 * Initializes internal state of BaseAlpzaComite object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id_alpza_comite] column value.
	 *
	 * @return     int
	 */
	public function getIdAlpzaComite()
	{
		return $this->id_alpza_comite;
	}

	/**
	 * Get the [id_alpza_miembro] column value.
	 *
	 * @return     int
	 */
	public function getIdAlpzaMiembro()
	{
		return $this->id_alpza_miembro;
	}

	/**
	 * Get the [nombre] column value.
	 *
	 * @return     string
	 */
	public function getNombre()
	{
		return $this->nombre;
	}

	/**
	 * Get the [mision] column value.
	 *
	 * @return     string
	 */
	public function getMision()
	{
		return $this->mision;
	}

	/**
	 * Get the [vision] column value.
	 *
	 * @return     string
	 */
	public function getVision()
	{
		return $this->vision;
	}

	/**
	 * Get the [objetivo] column value.
	 *
	 * @return     string
	 */
	public function getObjetivo()
	{
		return $this->objetivo;
	}

	/**
	 * Get the [informacion] column value.
	 *
	 * @return     string
	 */
	public function getInformacion()
	{
		return $this->informacion;
	}

	/**
	 * Get the [email] column value.
	 *
	 * @return     string
	 */
	public function getEmail()
	{
		return $this->email;
	}

	/**
	 * Get the [coordinador] column value.
	 *
	 * @return     string
	 */
	public function getCoordinador()
	{
		return $this->coordinador;
	}

	/**
	 * Get the [email_coordinador] column value.
	 *
	 * @return     string
	 */
	public function getEmailCoordinador()
	{
		return $this->email_coordinador;
	}

	/**
	 * Get the [estado] column value.
	 *
	 * @return     int
	 */
	public function getEstado()
	{
		return $this->estado;
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_ingreso] column value.
	 *
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaIngreso($format = 'Y-m-d')
	{
		if ($this->fecha_ingreso === null) {
			return null;
		}


		if ($this->fecha_ingreso === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_ingreso);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_ingreso, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_edicion] column value.
	 *
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaEdicion($format = 'Y-m-d')
	{
		if ($this->fecha_edicion === null) {
			return null;
		}


		if ($this->fecha_edicion === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_edicion);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_edicion, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_publicacion] column value.
	 *
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaPublicacion($format = 'Y-m-d')
	{
		if ($this->fecha_publicacion === null) {
			return null;
		}


		if ($this->fecha_publicacion === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha_publicacion);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_publicacion, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id_alpza_comite] column.
	 *
	 * @param      int $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setIdAlpzaComite($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_alpza_comite !== $v) {
			$this->id_alpza_comite = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::ID_ALPZA_COMITE;
		}

		return $this;
	} // setIdAlpzaComite()

	/**
	 * Set the value of [id_alpza_miembro] column.
	 *
	 * @param      int $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setIdAlpzaMiembro($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_alpza_miembro !== $v) {
			$this->id_alpza_miembro = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::ID_ALPZA_MIEMBRO;
		}

		if ($this->aAlpzaMiembro !== null && $this->aAlpzaMiembro->getIdAlpzaMiembro() !== $v) {
			$this->aAlpzaMiembro = null;
		}

		return $this;
	} // setIdAlpzaMiembro()

	/**
	 * Set the value of [nombre] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nombre !== $v) {
			$this->nombre = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::NOMBRE;
		}

		return $this;
	} // setNombre()

	/**
	 * Set the value of [mision] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setMision($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->mision !== $v) {
			$this->mision = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::MISION;
		}

		return $this;
	} // setMision()

	/**
	 * Set the value of [vision] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setVision($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->vision !== $v) {
			$this->vision = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::VISION;
		}

		return $this;
	} // setVision()

	/**
	 * Set the value of [objetivo] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setObjetivo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->objetivo !== $v) {
			$this->objetivo = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::OBJETIVO;
		}

		return $this;
	} // setObjetivo()

	/**
	 * Set the value of [informacion] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setInformacion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->informacion !== $v) {
			$this->informacion = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::INFORMACION;
		}

		return $this;
	} // setInformacion()

	/**
	 * Set the value of [email] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setEmail($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->email !== $v) {
			$this->email = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::EMAIL;
		}

		return $this;
	} // setEmail()

	/**
	 * Set the value of [coordinador] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setCoordinador($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->coordinador !== $v) {
			$this->coordinador = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::COORDINADOR;
		}

		return $this;
	} // setCoordinador()

	/**
	 * Set the value of [email_coordinador] column.
	 *
	 * @param      string $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setEmailCoordinador($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->email_coordinador !== $v) {
			$this->email_coordinador = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::EMAIL_COORDINADOR;
		}

		return $this;
	} // setEmailCoordinador()

	/**
	 * Set the value of [estado] column.
	 *
	 * @param      int $v new value
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setEstado($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->estado !== $v || $this->isNew()) {
			$this->estado = $v;
			$this->modifiedColumns[] = AlpzaComitePeer::ESTADO;
		}

		return $this;
	} // setEstado()

	/**
	 * Sets the value of [fecha_ingreso] column to a normalized version of the date/time value specified.
	 *
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setFechaIngreso($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_ingreso !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_ingreso !== null && $tmpDt = new DateTime($this->fecha_ingreso)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match
			)
			{
				$this->fecha_ingreso = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = AlpzaComitePeer::FECHA_INGRESO;
			}
		} // if either are not null

		return $this;
	} // setFechaIngreso()

	/**
	 * Sets the value of [fecha_edicion] column to a normalized version of the date/time value specified.
	 *
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setFechaEdicion($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_edicion !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_edicion !== null && $tmpDt = new DateTime($this->fecha_edicion)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match
			)
			{
				$this->fecha_edicion = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = AlpzaComitePeer::FECHA_EDICION;
			}
		} // if either are not null

		return $this;
	} // setFechaEdicion()

	/**
	 * Sets the value of [fecha_publicacion] column to a normalized version of the date/time value specified.
	 *
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     AlpzaComite The current object (for fluent API support)
	 */
	public function setFechaPublicacion($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_publicacion !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_publicacion !== null && $tmpDt = new DateTime($this->fecha_publicacion)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match
			)
			{
				$this->fecha_publicacion = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = AlpzaComitePeer::FECHA_PUBLICACION;
			}
		} // if either are not null

		return $this;
	} // setFechaPublicacion()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		if ($this->estado !== 1) {
			return false;
		}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id_alpza_comite = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_alpza_miembro = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->nombre = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->mision = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->vision = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->objetivo = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->informacion = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->email = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->coordinador = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->email_coordinador = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->estado = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->fecha_ingreso = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->fecha_edicion = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->fecha_publicacion = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 14; // 14 = AlpzaComitePeer::NUM_COLUMNS - AlpzaComitePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating AlpzaComite object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aAlpzaMiembro !== null && $this->id_alpza_miembro !== $this->aAlpzaMiembro->getIdAlpzaMiembro()) {
			$this->aAlpzaMiembro = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AlpzaComitePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = AlpzaComitePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aAlpzaMiembro = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AlpzaComitePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseAlpzaComite:delete:pre') as $callable)
			{
				if (call_user_func($callable, $this, $con))
				{
					$con->commit();
						
					return;
				}
			}

			if ($ret) {
				AlpzaComitePeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseAlpzaComite:delete:post') as $callable)
				{
					call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AlpzaComitePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseAlpzaComite:save:pre') as $callable)
			{
				if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
				{
					$con->commit();
						
					return $affectedRows;
				}
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseAlpzaComite:save:post') as $callable)
				{
					call_user_func($callable, $this, $con, $affectedRows);
				}

				AlpzaComitePeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAlpzaMiembro !== null) {
				if ($this->aAlpzaMiembro->isModified() || $this->aAlpzaMiembro->isNew()) {
					$affectedRows += $this->aAlpzaMiembro->save($con);
				}
				$this->setAlpzaMiembro($this->aAlpzaMiembro);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = AlpzaComitePeer::ID_ALPZA_COMITE;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = AlpzaComitePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
					// should always be true here (even though technically
					// BasePeer::doInsert() can insert multiple rows).

					$this->setIdAlpzaComite($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += AlpzaComitePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAlpzaMiembro !== null) {
				if (!$this->aAlpzaMiembro->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAlpzaMiembro->getValidationFailures());
				}
			}


			if (($retval = AlpzaComitePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AlpzaComitePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getIdAlpzaComite();
				break;
			case 1:
				return $this->getIdAlpzaMiembro();
				break;
			case 2:
				return $this->getNombre();
				break;
			case 3:
				return $this->getMision();
				break;
			case 4:
				return $this->getVision();
				break;
			case 5:
				return $this->getObjetivo();
				break;
			case 6:
				return $this->getInformacion();
				break;
			case 7:
				return $this->getEmail();
				break;
			case 8:
				return $this->getCoordinador();
				break;
			case 9:
				return $this->getEmailCoordinador();
				break;
			case 10:
				return $this->getEstado();
				break;
			case 11:
				return $this->getFechaIngreso();
				break;
			case 12:
				return $this->getFechaEdicion();
				break;
			case 13:
				return $this->getFechaPublicacion();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = AlpzaComitePeer::getFieldNames($keyType);
		$result = array(
		$keys[0] => $this->getIdAlpzaComite(),
		$keys[1] => $this->getIdAlpzaMiembro(),
		$keys[2] => $this->getNombre(),
		$keys[3] => $this->getMision(),
		$keys[4] => $this->getVision(),
		$keys[5] => $this->getObjetivo(),
		$keys[6] => $this->getInformacion(),
		$keys[7] => $this->getEmail(),
		$keys[8] => $this->getCoordinador(),
		$keys[9] => $this->getEmailCoordinador(),
		$keys[10] => $this->getEstado(),
		$keys[11] => $this->getFechaIngreso(),
		$keys[12] => $this->getFechaEdicion(),
		$keys[13] => $this->getFechaPublicacion(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AlpzaComitePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setIdAlpzaComite($value);
				break;
			case 1:
				$this->setIdAlpzaMiembro($value);
				break;
			case 2:
				$this->setNombre($value);
				break;
			case 3:
				$this->setMision($value);
				break;
			case 4:
				$this->setVision($value);
				break;
			case 5:
				$this->setObjetivo($value);
				break;
			case 6:
				$this->setInformacion($value);
				break;
			case 7:
				$this->setEmail($value);
				break;
			case 8:
				$this->setCoordinador($value);
				break;
			case 9:
				$this->setEmailCoordinador($value);
				break;
			case 10:
				$this->setEstado($value);
				break;
			case 11:
				$this->setFechaIngreso($value);
				break;
			case 12:
				$this->setFechaEdicion($value);
				break;
			case 13:
				$this->setFechaPublicacion($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = AlpzaComitePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setIdAlpzaComite($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdAlpzaMiembro($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setNombre($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setMision($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setVision($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setObjetivo($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setInformacion($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setEmail($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCoordinador($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setEmailCoordinador($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setEstado($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setFechaIngreso($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setFechaEdicion($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setFechaPublicacion($arr[$keys[13]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(AlpzaComitePeer::DATABASE_NAME);

		if ($this->isColumnModified(AlpzaComitePeer::ID_ALPZA_COMITE)) $criteria->add(AlpzaComitePeer::ID_ALPZA_COMITE, $this->id_alpza_comite);
		if ($this->isColumnModified(AlpzaComitePeer::ID_ALPZA_MIEMBRO)) $criteria->add(AlpzaComitePeer::ID_ALPZA_MIEMBRO, $this->id_alpza_miembro);
		if ($this->isColumnModified(AlpzaComitePeer::NOMBRE)) $criteria->add(AlpzaComitePeer::NOMBRE, $this->nombre);
		if ($this->isColumnModified(AlpzaComitePeer::MISION)) $criteria->add(AlpzaComitePeer::MISION, $this->mision);
		if ($this->isColumnModified(AlpzaComitePeer::VISION)) $criteria->add(AlpzaComitePeer::VISION, $this->vision);
		if ($this->isColumnModified(AlpzaComitePeer::OBJETIVO)) $criteria->add(AlpzaComitePeer::OBJETIVO, $this->objetivo);
		if ($this->isColumnModified(AlpzaComitePeer::INFORMACION)) $criteria->add(AlpzaComitePeer::INFORMACION, $this->informacion);
		if ($this->isColumnModified(AlpzaComitePeer::EMAIL)) $criteria->add(AlpzaComitePeer::EMAIL, $this->email);
		if ($this->isColumnModified(AlpzaComitePeer::COORDINADOR)) $criteria->add(AlpzaComitePeer::COORDINADOR, $this->coordinador);
		if ($this->isColumnModified(AlpzaComitePeer::EMAIL_COORDINADOR)) $criteria->add(AlpzaComitePeer::EMAIL_COORDINADOR, $this->email_coordinador);
		if ($this->isColumnModified(AlpzaComitePeer::ESTADO)) $criteria->add(AlpzaComitePeer::ESTADO, $this->estado);
		if ($this->isColumnModified(AlpzaComitePeer::FECHA_INGRESO)) $criteria->add(AlpzaComitePeer::FECHA_INGRESO, $this->fecha_ingreso);
		if ($this->isColumnModified(AlpzaComitePeer::FECHA_EDICION)) $criteria->add(AlpzaComitePeer::FECHA_EDICION, $this->fecha_edicion);
		if ($this->isColumnModified(AlpzaComitePeer::FECHA_PUBLICACION)) $criteria->add(AlpzaComitePeer::FECHA_PUBLICACION, $this->fecha_publicacion);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(AlpzaComitePeer::DATABASE_NAME);

		$criteria->add(AlpzaComitePeer::ID_ALPZA_COMITE, $this->id_alpza_comite);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getIdAlpzaComite();
	}

	/**
	 * Generic method to set the primary key (id_alpza_comite column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setIdAlpzaComite($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of AlpzaComite (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdAlpzaMiembro($this->id_alpza_miembro);

		$copyObj->setNombre($this->nombre);

		$copyObj->setMision($this->mision);

		$copyObj->setVision($this->vision);

		$copyObj->setObjetivo($this->objetivo);

		$copyObj->setInformacion($this->informacion);

		$copyObj->setEmail($this->email);

		$copyObj->setCoordinador($this->coordinador);

		$copyObj->setEmailCoordinador($this->email_coordinador);

		$copyObj->setEstado($this->estado);

		$copyObj->setFechaIngreso($this->fecha_ingreso);

		$copyObj->setFechaEdicion($this->fecha_edicion);

		$copyObj->setFechaPublicacion($this->fecha_publicacion);


		$copyObj->setNew(true);

		$copyObj->setIdAlpzaComite(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     AlpzaComite Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     AlpzaComitePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new AlpzaComitePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a AlpzaMiembro object.
	 *
	 * @param      AlpzaMiembro $v
	 * @return     AlpzaComite The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAlpzaMiembro(AlpzaMiembro $v = null)
	{
		if ($v === null) {
			$this->setIdAlpzaMiembro(NULL);
		} else {
			$this->setIdAlpzaMiembro($v->getIdAlpzaMiembro());
		}

		$this->aAlpzaMiembro = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the AlpzaMiembro object, it will not be re-added.
		if ($v !== null) {
			$v->addAlpzaComite($this);
		}

		return $this;
	}


	/**
	 * Get the associated AlpzaMiembro object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     AlpzaMiembro The associated AlpzaMiembro object.
	 * @throws     PropelException
	 */
	public function getAlpzaMiembro(PropelPDO $con = null)
	{
		if ($this->aAlpzaMiembro === null && ($this->id_alpza_miembro !== null)) {
			$this->aAlpzaMiembro = AlpzaMiembroPeer::retrieveByPk($this->id_alpza_miembro);
			/* The following can be used additionally to
			 guarantee the related object contains a reference
			 to this object.  This level of coupling may, however, be
			 undesirable since it could result in an only partially populated collection
			 in the referenced object.
			 $this->aAlpzaMiembro->addAlpzaComites($this);
			 */
		}
		return $this->aAlpzaMiembro;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

		$this->aAlpzaMiembro = null;
	}

	// symfony_behaviors behavior

	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
		if (!$callable = sfMixer::getCallable('BaseAlpzaComite:'.$method))
		{
			throw new sfException(sprintf('Call to undefined method BaseAlpzaComite::%s', $method));
		}

		array_unshift($arguments, $this);

		return call_user_func_array($callable, $arguments);
	}

} // BaseAlpzaComite
